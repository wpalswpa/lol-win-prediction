# 파이썬
__pycache__/
*.pyc
.ipynb_checkpoints/

# 원본 데이터 — 라이선스상 재배포 금지 + 용량(97MB). 받는 법은 data/README.md
data/*.csv

# OS
.DS_Store
Thumbs.db

# 로컬 백업 (저장소에는 불필요)
docs/_archive/

# 로컬 실행 환경·런타임 (커밋 금지)
.env
venv/
venv311/
run/
logs/
*.log
node_modules/

# 패키지 빌드 부산물
*.egg-info/
build/
dist/
# 챔피언 통계 수집 원본 — 크고 재생성 가능하며 공개 Riot ID 를 담는다
data/champion_raw*

# 랭킹 이름 캐시 — PUUID 를 담으므로 공개 저장소에 올리지 않는다
data/ranking_names.jsonl
data/tier_raw.jsonl
db/votes.sqlite3

# PowerPoint 잠금 파일 (열려 있는 동안 생겼다 사라진다)
~$*

# 초안·로컬 캐시 (저장소에는 불필요)
F.json
presentation_draft.pptx

# models3 후보 모델 산출물 — train_candidate.py 가 다시 만든다 (채택 모델 사본은 커밋)
models3/*/build/

# 모델 API 서버 전용 가상환경 (./check_api.sh setup 이 만든다)
models/.venv/
models/review_queue.jsonl
